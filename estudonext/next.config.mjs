const nextConfig = {
    rewrites: () => {
        return [
            {
                source: "/pokedex",
                destination: "/pokedex"
            }
        ]
    }
};

export default nextConfig;
